{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for Etta: dialog (incl. save) plus a WRITE-ONLY text-file grant scoped to the user's standard export destinations (Downloads/Documents/Desktop) for the export flow. The webview has no fs read/mkdir/exists commands at all, and $APPDATA (etta.db, canonical answer keys) is not in the fs scope. No shell access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-save",
    {
      "identifier": "fs:scope",
      "allow": [
        { "path": "$DOWNLOAD" },
        { "path": "$DOWNLOAD/**" },
        { "path": "$DOCUMENT" },
        { "path": "$DOCUMENT/**" },
        { "path": "$DESKTOP" },
        { "path": "$DESKTOP/**" }
      ]
    },
    "fs:allow-write-text-file",
    "updater:default"
  ]
}
